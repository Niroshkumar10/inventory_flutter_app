import 'package:flutter/material.dart';

class AppColors {
  // 🌿 Primary Brand Color (Trust + Money + Growth)
  static const primary = Color(0xFF1E7F5C); // Deep Teal Green

  // 🌱 Secondary Accent
  static const secondary = Color(0xFF4CAF50); // Soft Green

  // 🧱 Background
  static const background = Color(0xFFF5F7FA); // Light Grey Blue

  // 📄 Surface (Cards, Inputs)
  static const surface = Colors.white;

  // ❗ Error / Danger
  static const error = Color(0xFFD32F2F);

  // 🔤 Text
  static const textPrimary = Color(0xFF1C1C1C);
  static const textSecondary = Color(0xFF6B7280);

  // 💰 Status Colors
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFF9A825);
  static const info = Color(0xFF0288D1);
}
